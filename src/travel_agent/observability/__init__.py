"""Week 19 deliverable: structured logging, Prometheus metrics, LLM cost
tracking, and Sentry error tracking. See each module's docstring; LangSmith
tracing needs no code here — LangChain reads `LANGCHAIN_TRACING_V2`/
`LANGCHAIN_API_KEY`/`LANGCHAIN_PROJECT` directly from the environment
`config.py`'s `load_dotenv()` already populates.
"""
